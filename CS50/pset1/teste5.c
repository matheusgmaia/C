#include <stdio.h>
#include <stdlib.h>
#include <cs50.h>
#include <string.h>
#include <ctype.h>

int main(void)
{
    p = GetString();
    if(int isalpha(p)=0)
    {
    printf("ok");
    printf("%s", p)
    }
    
}
    int s =((((r - 'a')+key[i])%26)+'a');
    printf("%c", s);    
    
       for (int i = 0, n = strlen(p); i < n; i++)
    int k = key[i];
    printf ("%c", k)
    
    for (int i = 0, n = strlen(p); i < n; i++)
    {
    int r = tolower(p[i]);
    printf("%c", r);
    }
